AIE_Framework
=============
ALWAYS DO A PULL FIRST!!!!


create a Git repository of your directory:

git init
git add .
git commit -a -m "FIRST"

on your flashy do

git clone --bare /path/to/my/AIEFrameworkdir

then whenever you want to make a computer sided copy, go to some directory(desktop) and run this 

git clone /path/to/stick/repo